name: crysterm
version: 1.0.0

authors:
  - Davor Ocelic <docelic@crystallabs.io>

crystal: ">= 1.21.0"

license: AGPLv3

# Note: the `crysterm` CLI (src/bin/crysterm.cr) is intentionally NOT a
# build target — it only works with the remote subsystem. Build it explicitly:
#   crystal build src/bin/crysterm.cr -Dremote -o bin/crysterm

dependencies:
  tput:
    github: crystallabs/tput
    version: ~> 2.0
  crystallabs-helpers:
    github: crystallabs/crystallabs-helpers.cr
    version: ~> 1.1
  term_colors:
    github: crystallabs/term_colors
    version: ~> 2.0
  w3m_image_display:
    github: SamualLB/w3mimagedisplay
  event_handler:
    github: crystallabs/event_handler
    version: ~> 2.0
  gpm:
    github: crystallabs/gpm.cr
    version: ~> 2.0
  pnggif:
    github: crystallabs/pnggif
    version: ~> 0.1
  superconf:
    github: crystallabs/superconf.cr
    version: ~> 0.1
  html5:
    github: naqvis/crystal-html5
  markd:
    github: icyleaf/markd

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    branch: master
